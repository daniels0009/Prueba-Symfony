{% extends 'base.html.twig' %}

{% block title %}Todas los Blocks{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h2>{{task.title}} ✅</h2>
    <p class="data-task">
        {{task.user.name }} {{task.user.surname }} 
         | {{task.createdAt|date('d/m/Y h:i') }} | 
        {% if task.topic == 'Matematicas' %}
              {{ 'Educacion' }}
        {% endif %}
        {% if task.topic == 'Ciencia' %}
            {{ 'Educacion' }}
        {% endif %}
        {% if task.topic == 'Quimica' %}
            {{ 'Educacion' }}
        {% endif %}
        {% if task.topic == 'Programacion' %}
            {{ 'Programacion' }}
        {% endif %}
        {% if task.topic == 'Bigdata' %}
            {{ 'Programcion' }}
        {% endif %}
        {% if task.topic == 'IA' %}
            {{ 'Programacion' }}
        {% endif %} 
         | {{task.words}} Palabras
    </p>
    <p>
        {{task.content}}
    </p>
 
</div>
{% endblock %}
